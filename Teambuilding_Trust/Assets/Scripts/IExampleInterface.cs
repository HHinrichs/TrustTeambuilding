public interface IExampleInterface // Always start with I and use camel case
{
    void InterfaceDoSomething(); // Pascal case and verbs for interface functions

    // Only combine interface functions if they are needed together
}
